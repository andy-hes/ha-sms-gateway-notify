{
  "config": {
    "step": {
      "user": {
        "title": "SMS Gateway Notify",
        "description": "Connect Home Assistant to your SMS gateway.",
        "data": {
          "name": "Name",
          "base_url": "Gateway URL",
          "api_key": "API key",
          "recipients": "Default recipients (comma-separated)"
        }
      }
    },
    "error": {
      "invalid_url": "Invalid URL",
      "required": "Required field"
    },
    "abort": {
      "already_configured": "This gateway is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "SMS Gateway Notify options",
        "data": {
          "base_url": "Gateway URL",
          "api_key": "API key",
          "recipients": "Default recipients (comma-separated)"
        }
      }
    }
  }
}
